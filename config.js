module.exports = {
  // *INFO: client's token
  clientId: process.env.CLIENT_ID,
  // *INFO: bot's token
  botToken: process.env.BOT_TOKEN,
  // *INFO: server's token
  guildId: process.env.GUILD_ID,
};
